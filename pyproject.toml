[project]
name = "mark-six-history"
version = "0.1.0"
description = "Fetch Hong Kong Mark Six lottery history"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "beautifulsoup4>=4.11.0",
    "pandas>=1.5.0",
]

[project.scripts]
fetch-mark-six = "fetch_data:main"
